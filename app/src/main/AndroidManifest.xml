<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Android Auto. The surface permission is what lets the app draw its own map on the
         car screen rather than handing the host a list of cards. -->
    <uses-permission android:name="androidx.car.app.NAVIGATION_TEMPLATES" />
    <uses-permission android:name="androidx.car.app.ACCESS_SURFACE" />
    <!-- For the point-of-interest probe service below, which is not allowed the navigation
         templates and so draws with the map/content ones instead. -->
    <uses-permission android:name="androidx.car.app.MAP_TEMPLATES" />

    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <application
        android:name=".ShadowGpsApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShadowGps">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="fullUser"
            android:theme="@style/Theme.ShadowGps">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Accept "navigate to" hand-offs from other apps, the way a maps app should. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="geo" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />
        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="1" />

        <service
            android:name=".car.ShadowCarAppService"
            android:label="@string/car_app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.NAVIGATION" />
            </intent-filter>

            <!-- Accept "navigate here" from other car apps, the way the phone side already
                 accepts geo: links. A navigation app that cannot be handed a destination by
                 the car is only half connected to it. -->
            <intent-filter>
                <action android:name="androidx.car.app.action.NAVIGATE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="geo" />
            </intent-filter>
        </service>

        <!--
          Diagnostic, to be removed once it has answered its question.

          Android Auto lists neither Waze's nor Maps' problem: ShadowGPS is absent from
          Customize launcher altogether, and the merged manifest that CI prints shows nothing
          wrong with how the navigation service above is declared. Registering the same app a
          second time under the point-of-interest category — which has none of the navigation
          category's rules about where an app was installed from — separates "the host never
          sees this app" from "the host sees it and will not list a sideloaded navigation
          app". Whichever entry turns up in the launcher is the answer.
        -->
        <service
            android:name=".car.PoiProbeCarAppService"
            android:label="@string/car_poi_probe_name"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.POI" />
            </intent-filter>
        </service>

        <service
            android:name=".nav.NavigationService"
            android:exported="false"
            android:foregroundServiceType="location" />
    </application>
</manifest>
